package com.example.cuidafamily.ui.theme

import androidx.compose.ui.graphics.Color

// Paleta de colores Premium "Vibrante" para CuidaFamily

// Acento Principal / Botones (Rosa -> Violeta)
val GradientStarStart = Color(0xFFFF6B9D)
val GradientStarEnd = Color(0xFF7C4DFF)

// Administrador Familiar (Morado -> Violeta Claro)
val AdminGradientStart = Color(0xFF7C4DFF)
val AdminGradientEnd = Color(0xFFB794FF)
val RoleAdminColor = AdminGradientStart

// Colaborador (Verde Esmeralda -> Menta)
val CollabGradientStart = Color(0xFF00C896)
val CollabGradientEnd = Color(0xFF4EE0B8)
val RoleCollabColor = CollabGradientStart

// Cuidador Externo (Coral -> Naranja Suave)
val CuidadorGradientStart = Color(0xFFFF7A45)
val CuidadorGradientEnd = Color(0xFFFFB088)
val RoleCuidadorColor = CuidadorGradientStart

// Fondos y Blobs
val BgStart = Color(0xFFF3E8FF)
val BgEnd = Color(0xFFFFF5F0)
val BlobPink = Color(0xFFFF6B9D)
val BlobTurquoise = Color(0xFF5EEAD4)

// Alertas y Éxito
val RojoAlerta = Color(0xFFD32F2F)
val RojoAlertaFondo = Color(0xFFFFEBEE)
val AmbarAlerta = Color(0xFFE65100)
val AmbarAlertaFondo = Color(0xFFFFF3E0)
val VerdeExito = Color(0xFF00C896) // Usamos el verde vibrante de colaborador
val VerdeExitoFondo = Color(0xFFE8F5E9)

// Generales
val LavandaPrimary = Color(0xFF6C5CE7)
val LavandaPrimaryLight = Color(0xFFEDE9FE)
val AzulSecundario = Color(0xFF4A90E2)
val FondoNeutro = Color(0xFFFFFFFF)
val TextoSecundario = Color(0xFF6B7280)

// Aliases para compatibilidad con código previo
val GradientStart = GradientStarStart
val GradientEnd = GradientStarEnd
val RoleAdminBg = Color(0xFFE1D5FF)
val RoleCollabBg = Color(0xFFC8FFEF)
val RoleCuidadorBg = Color(0xFFFFE1D0)
